<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- 部品単位のテスト（フォルダの入れ替え、候補の並びなど）のため。 -->
    <InternalsVisibleTo Include="ZmkOverlay.Core.Tests" />
  </ItemGroup>

</Project>
