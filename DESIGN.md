# ZMK Keymap Overlay — 設計メモ

ZMK キーマップを Windows 画面上に半透過オーバーレイ表示するツール。

## 次にやること

日常使いできる状態まで来ている。残っているのは以下。

| | 内容 | 前提 |
|---|---|---|
| 1 | **押下キーのハイライト** | 方式の決定が先。「押下キーのハイライト」の節を参照 |
| 2 | ~~設定 UI~~ | 設定画面として実装済み（「設定画面」の節） |
| 3 | **会社 PC での確認（V4）** | 未署名 exe の実行可否と配置先。利用者の判断で保留中 |
| 4 | 手動レイヤー経路の不安定さ | 未解決。「検証」の節を参照 |
| 5 | **GitHub での公開** | リポジトリの作成・push・Releases への zip の設置。利用者が中身を見てから決める（「公開」の節） |
| 6 | ~~生成した書き換えで実際にビルドできるかの確認~~ | 確認済み。書き換え版の Pyuron を実機に書き込んでも問題なし（2026-09-19。「公開前の見直し」の節の「ビルドの確認」） |
| 7 | AltGr 配列での判定の実機確認 | ドイツ語配列などを入れた PC で、既定のショートカットが避けられることを確かめる（「AltGr と衝突しないショートカット」の節） |
| 8 | **キーボードの書き換えの「試験的」を外すか** | 公開後、報告が基準を満たしたら（「公開」の節の「キーボードの書き換えは試験的として出す」） |

一般公開に向けた UI と導入体験の作り直しを進めている（.NET 10 化・日英対応・見た目・トレイ・
設定画面・初回セットアップ・キーマップ書き換えの生成）。

## 公開前の見直し（2026-09-17）

公開前に全体を見直し、次を直した。それぞれの理由は各節に書いた。

| 見つかったこと | 直し方 | 節 |
|---|---|---|
| 書き換え済みのキーマップでも「書き換えが必要」と出続ける | 書き換え後の文字列が今のファイルと同じかで判断する | 書き換えの生成 |
| キーマップが読めないと、設定画面も開けずに終了する | サンプルで起動して選び直してもらう。壊れた設定ファイルは退避する | 起動できなくならないように |
| 2 重起動でホットキーが取れず、警告が並ぶ | 1 つだけ常駐し、2 つ目は設定画面を開かせて終わる | 常駐は 1 つだけ |
| Ctrl などを押したままレイヤーに入ると表示されない | 合図キーを修飾キーの全組み合わせで押さえる | レイヤー連動 PC 側 |
| Ctrl+Alt の既定ショートカットが AltGr の文字入力を奪う | 文字が出る組み合わせは登録しない | AltGr と衝突しないショートカット |
| 標準の Corne が左右に分かれず、親指の段もずれる | 推定を直し、ZMK 本体から配置を取れるようにする | 物理レイアウト |
| トップレベルの `/delete-node/` 以降が黙って読み捨てられる | 読み飛ばして先へ進む | 読まないもの |
| 配列の既定が誰にでも JIS | 初回起動時に PC のキーボードから決める | 配列依存 |
| Pyuron のファイルのライセンスが分からない | 公開物と履歴から外し、サンプルを Corne、テストを自作データにした | 公開 |
| 書き換えを実機で確かめたのは作者の手作業版だけで、ビルドが失敗したときの戻し方も無い | 「試験的」と出し、書き換えを取り除く操作と報告の受け口を足した | 公開 |
| 合図キーが別の PC で働く（Mac の F14 / F15、Linux の F20 / F21） | その 4 つを最後に割り当てる | 書き換えの生成 |
| 「L1 以上のときだけ表示」で、前に出したレイヤーが一瞬見えてから切り替わる | 中身を消した絵を描かせてから隠す | オーバーレイ実装の要点 |

### ビルドの確認

生成した書き換えで GitHub Actions のビルドが通るかを、非公開のテスト用リポジトリ（`utori7/zmk-overlay-buildtest`）で確かめた（2026-09-17）。
どれもアプリの書き換え処理（`KeymapPatcher`）が出力したキーマップを、そのままコミットしてビルドした。

| ブランチ | ZMK | 中身 | 結果 |
|---|---|---|---|
| `corne-stock` | v0.3 | ZMK 本体の標準 Corne のキーマップ。`&mo` が合図付きマクロに変わる | 左右とも成功 |
| `corne-demo` | v0.3 | 同梱サンプルの元（`&lt`、ホームロウモッド、条件付きレイヤー、コンボ）に `&lt { quick-tap-ms }` の上書きを足したもの。合図付きの hold-tap と、上書きの写しが入る | 左右とも成功 |
| `pyuron` | main | 作者の Pyuron（手で入れた合図キーを外してから生成）。ZMK Studio を有効にした右手（`studio-rpc-usb-uart`）を含む | 3 つとも成功 |

`display-name` を付けた生成ビヘイビアも、ZMK Studio を有効にしたビルドで問題ないことが分かった。
テスト用リポジトリは、その後削除した。

**実機での確認。** Pyuron のリポジトリ（`utori7/zmk-config-Pyuron`、公開）に、`paw3220` から `overlay-generated-test` ブランチを作り、
`config/Pyuron.keymap` だけを生成版に差し替えた（2026-09-17）。手で入れた合図キー（`#include "layer-signal.dtsi"` と `&lt_sym` などの名前）を
外してから、割り当て順を変える前の `KeymapPatcher` で生成したもので、合図キーは手作業版と同じ F13〜F16。PC 側の設定を変えずに確かめられる。
ビルドは 3 つとも成功した。

右手に書き込んで確かめた（2026-09-19）。親指の 4 キーのタップ、Space を押したままのカーソル移動、英数を押したままの数字、
離したあとの通常の入力、オーバーレイの追従（L1 / L2 / L3 / L5）、設定画面の合図の ✓ のすべてで問題なかった。

手作業版との違いは、名前と `display-name` だけではない。リポジトリの手作業版（`config/layer-signal.dtsi`）は、
直す前の `flavor = "hold-preferred"` と `label` のままになっている。生成版は組み込みの `&lt` と同じ `tap-preferred` なので、
押し心地はオーバーレイを入れる前に戻る（「ZMK 側」の節）。

## 要件

| # | 要件 | 備考 |
|---|---|---|
| R1 | 半透過オーバーレイの表示/非表示 | 最前面・クリックスルー・**フォーカスを奪わない** |
| R2 | ホットキーでトグル | |
| R3 | レイヤー連動 | ZMK 側から合図キーを送る方式（案A） |
| R4 | **ユーザー権限のみで導入可能** | 管理者権限・インストーラ不要 |
| R5 | 汎用設計 | 特定キーボードに依存しない |

## 技術選定

- **C# / .NET 10 / WPF**（理由は「.NET のバージョン」の節）
- 配布は 2 形態をビルドできるようにする
  - `self-contained + PublishSingleFile` — exe 1 個。ランタイム不要。約 150MB
  - `framework-dependent` — 数 MB。.NET デスクトップランタイムが必要
- 設定は **ポータブル優先**：exe と同じフォルダに `config.json` があればそれを、無ければ `%APPDATA%\ZmkOverlay\` を使う

> WPF は `PublishTrimmed` 非対応。サイズ削減はできない。

## アーキテクチャ

```
ZmkOverlay.Core/          ← UI 非依存。CLI とテストから叩ける
├─ Dts/                   devicetree パーサ
│   ├─ Preprocessor       #define / #include 展開
│   ├─ DtsLexer
│   └─ DtsParser          → ノードツリー
├─ Model/
│   ├─ PhysicalLayout     キーの物理位置
│   ├─ Keymap / Layer / Binding
├─ Bindings/
│   ├─ BehaviorFormatter  &kp &mo &lt &mt &sk &trans &none &bt ...
│   └─ KeycodeTable       ZMK キーコード → 表示ラベル + Windows VK
└─ Config/

ZmkOverlay.App/           ← WPF (net10.0-windows)
├─ Overlay/OverlayWindow  半透過・クリックスルー
├─ Render/KeymapCanvas    レイアウト描画
├─ Interop/HotKeyService  RegisterHotKey + WM_HOTKEY
├─ Interop/KeyStateWatcher GetAsyncKeyState ポーリング（リリース検出）
├─ TrayIcon
└─ Settings/

ZmkOverlay.Tests/
```

## データモデル

### 物理レイアウト

ZMK 公式の physical layout（`key_physical_attrs`）に合わせる。単位は 1/100u。
これを採用することで、ZMK 側の定義をそのまま読める＝汎用性が担保される。

```
&key_physical_attrs <w> <h> <x> <y> <r> <rx> <ry>
```

```json
{
  "name": "corne",
  "keys": [
    { "pos": 0, "x": 0,   "y": 40, "w": 100, "h": 100, "r": 0, "rx": 0, "ry": 0 }
  ]
}
```

取り込み経路（優先順。`ZmkKeymapReader.ResolveLayout`）:
1. 設定で指定したファイル（`zmk.physicalLayoutFile`）
2. キーマップ自身に書かれた `zmk,physical-layout`
3. キーマップのフォルダ以下の `.dtsi` / `.overlay` から、キー数の合うもの（シールドの定義など）
4. ZMK 本体から取ってきたシールドの定義（`zmk.shieldLayoutFolder`。「ZMK 本体から取る」の節）
5. キーマップの書き方からの推定（`LayoutGuesser`）
6. 手書き JSON（ZMK のソースを使わない場合）

一般の利用者に「シールドの .dtsi を指定してください」と求めるのは難しいので、3〜5 を足した。
どこから得たかは設定画面に出す。推定だと分からないまま使うと、配置の違いに気づけない。

1 つのファイルに複数のレイアウトがあることがある（Corne の 5 列 / 6 列、キーボードの版違いなど）。
`chosen { zmk,physical-layout = &x; }` で選ばれていてキー数が合うもの → キー数が合う最初のもの → chosen のもの
→ 最初のもの、の順で選ぶ。以前は最初の 1 つしか見ず、42 キーのキーマップに 36 キーの定義を当てて候補から外していた。

回転角は `(-2400)` のように括弧つきで書かれる。以前はこれを数値と読めず、以降の値がずれていた。

**推定のしかた。** ZMK のキーマップは、たいてい 1 行 = キーボードの横一列で書かれ、分割キーボードなら
左右の手のあいだを大きな空白で空けてある。

- 空白がふつうの空白（全体の中央値）の 3 倍以上、かつ 3 文字以上なら、左右の分かれ目の候補にする。
  以前は下限が 6 文字で、標準の Corne（手のあいだが 3 文字）が分割と認識されなかった
- 候補が複数あれば、行の中央に近いものを選ぶ。標準の Corne の 1 段目は、TAB の後にも同じ幅の空白がある
- 分かれ目が見つかった行が半分に満たなければ、たまたま広い空白があっただけとみなして分割しない
- 分かれ目が書かれていない偶数個の行は真ん中で分ける（左右を詰めて書いた行）
- 左手は左端から、右手は右端に揃えて並べる
- いちばん長い行より字下げして書かれた短い行（Corne の親指の段）は、左右とも手のあいだ寄りに置く。
  分割していないキーボードの字下げした短い行（スペースキーの段など）は中央に置く

テスト用の demo40 は全キーが実物と一致し、標準の Corne も親指の段まで実物に近い形になる。
1 行に 17 個以上並ぶ書き方は推定しない。

位置を保ったまま読むために、コメントは消さずに同じ長さの空白で塗る（`SourceText.MaskComments`）。
プリプロセッサのコメント除去は文字を詰めるので、桁が変わってしまう。

### ZMK 本体から取る

Corne・Lily58・Sofle・Kyria のような定番のキーボードは、シールドの定義が利用者の zmk-config ではなく
ZMK 本体（zmkfirmware/zmk）にある。キーマップの近くを探しても見つからず、推定では実物と合わない。
しかも定義は `#include <layouts/foostan/corne/6column.dtsi>` のように ZMK の共有レイアウトを読み込んでいて、
利用者がシールドの .dtsi だけを手に入れても読めない。そこで ZMK 本体から取ってくる（`ZmkShieldSource`）。

- どのキーボードかは zmk-config の `build.yaml` の `shield` から分かる。`_left` / `_right` を外し、
  `nice_view` のような追加部品は後ろに回す。`kyria_rev3` のようにフォルダ名と違うものは、後ろの `_xxx` を外して試す
- ZMK の版は `config/west.yml` の zmk の `revision`。その版に無ければ `main` で取り直す（古い版には物理レイアウトの定義が無い）
- 取るのはシールドのフォルダの `.dtsi` / `.overlay` と、それが読み込む `app/dts/layouts/...` だけ。
  ZMK のソースと同じ並びで `cache/zmk/<シールド>@<版>/app/...` に置く
- プリプロセッサは `#include <layouts/...>` だけ、読み込み元から上へたどって `dts/` の下を探す。
  PC にある zmk のソースの中の .dtsi を選んだときも同じ仕組みで読める。
  ほかのシステムヘッダ（`<behaviors.dtsi>` など）は読まない。組み込みのビヘイビア定義が混ざると解釈が変わる
- 通信は、利用者が「ZMK 本体から取得」を押したときと、GitHub から取得したときに zmk-config の中にキーの並びが無かったときだけ。
  PC の .keymap を選んで配置が分からなかったときは、build.yaml からキーボードが分かれば、取りに行くかを尋ねる
- 取れなくても、キーマップは推定で表示して続ける。理由は画面に出す
- GitHub API は多くても数回（フォルダの一覧）。本文は raw.githubusercontent.com から取る

実際の ZMK 本体で Corne・Lily58・Sofle・Kyria（`kyria_rev3_left` から `kyria` に辿り着く）を取り、
それぞれの標準キーマップと組み合わせて、推定ではなく取得した配置で読めることを確かめた（2026-09-17）。

### GitHub から読む

ZMK の利用者には、zmk-config を GitHub のブラウザ画面だけで編集していて、PC にファイルを持っていない人が多い。
そこで設定画面に URL を貼って取得できるようにした（`GitHubSource` / `GitHubSync`）。

- ブラウザのアドレス欄からコピーしてくる形をひととおり受け付ける（`owner/repo` だけでもよい）
- GitHub API は多くても 2 回（既定のブランチとファイル一覧）。本文は raw.githubusercontent.com から取る。
  認証しないので公開リポジトリだけで、API は 1 時間に 60 回まで
- 取るのは、キーマップのあるフォルダ以下の `.keymap` / `.dtsi` / `.overlay` / `.dts` / `.h` だけ。
  シールドの定義も config/ 以下にあるので、保存したあとは「キーマップのフォルダ以下から物理レイアウトを探す」がそのまま効く
- 保存先は設定ファイルの隣の `cache/github/<owner>/<repo>/`。`zmk.keymapFile` はそこを設定ファイルからの相対で指すので、
  読み込み自体はローカルと同じで、フォルダごと持ち運んでも読める
- どのキーボードか（ルートの `build.yaml`）と ZMK の版（`west.yml`）も取る。キーの並びを ZMK 本体から取るときに使う
- いったん `.downloading` に全部取ってから入れ替える。途中で通信が切れても前回の保存分は壊れない。
  入れ替えは、前回の分を `.old` に退避 → 新しい分を移す → 退避分を消す、の順で、移せなければ退避分を戻す
- 取得はアプリ全体で 1 つずつ流す。初期設定・設定画面・トレイの再読み込みが同時に動くと、同じ一時フォルダを取り合う
- プロキシが HTML を返したり、JSON の形が想定と違ったりしても、例外で落とさず「想定外の応答」と伝える。
  403 は、`X-RateLimit-Remaining: 0` のときだけ回数制限と言う（ほかの 403 まで回数制限と言うと、待っても直らない）
- **通信するのは利用者が「取得」「取り直す」を押したときと、トレイの再読み込みだけ。** 起動時には通信しない。
  会社 PC で外部への通信が遮られていても、保存済みのもので動き続けるように。再読み込みで取り直せなかったときは知らせて、保存済みのもので続ける

### キーマップ

```json
{
  "layout": "corne",
  "layers": [
    { "index": 0, "name": "BASE", "bindings": ["&kp Q", "&kp W", "..."] }
  ]
}
```

### ラベル解決

キーは **tap（中央・大）** と **hold（左上・小）** の 2 段で描く。

| binding | hold | tap |
|---|---|---|
| `&kp Q` | | Q |
| `&mo 1` | | L1 |
| `&lt 2 SPACE` | L2 | Space |
| `&hml LGUI A` | Win | A |
| `&htp DEL F10` | Del | F10 |
| `&trans` | | ▽ |
| `&none` | | — |

`KeycodeTable` は、ZMK の `dt-bindings/zmk/keys.h` の名前を元に**手で書いた**内蔵表。
ZMK のソースが手元に無くても読めるようにするため。表に無い名前はそのまま表示する。

### 配列依存（JIS / US）

Pyuron のキーマップは JIS 配列前提で、`#define JP_LPAR LS(N8)` のような定義を 20 個以上持つ。
ZMK は US 位置の HID コードを送り、OS 側の配列で出力文字が決まるため、
**同じ binding でも JIS と US で表示すべき文字が違う**。

| binding | US 表示 | JIS 表示 |
|---|---|---|
| `LS(N8)` | `*` | `(` |
| `LBKT` | `[` | `@` |
| `LS(N2)` | `@` | `"` |

`KeycodeTable` に配列ごとのシフト面テーブルを持ち、設定 `keyboardLayout: "jis" \| "us"` で切り替える。

初回起動時の値は PC のキーボードから決める（`GetKeyboardType(0)` が 7 なら日本語キーボードで `jis`、それ以外は `us`）。
以前は誰にでも `jis` で、英語配列の人に最初から違う記号が出ていた。表示言語では決めない。
日本語の Windows に英語配列のキーボードを使う人もいるため。

## オーバーレイ実装の要点

WPF `Window`:
```
WindowStyle=None  AllowsTransparency=True  Background=Transparent
Topmost=True  ShowInTaskbar=False  ResizeMode=NoResize
```

`SourceInitialized` で拡張ウィンドウスタイルを追加:

| フラグ | 値 | 目的 |
|---|---|---|
| `WS_EX_TRANSPARENT` | 0x20 | クリックスルー |
| `WS_EX_NOACTIVATE` | 0x08000000 | **フォーカスを奪わない（最重要）** |
| `WS_EX_TOOLWINDOW` | 0x80 | Alt+Tab に出さない |

- マルチモニタ: `MonitorFromPoint` でカーソルのあるモニタに表示
- DPI: app.manifest で PerMonitorV2
- `AllowsTransparency=True` が重い場合は `UpdateLayeredWindow` に差し替える（表示領域を必要最小限にすれば実用上問題ない見込み）
- **隠すときは、中身を消した絵を描かせてから隠す**（`OverlayWindow.HideOverlay`）。透過ウィンドウは隠しても最後に描いた絵を持っていて、
  隠しているあいだは描き直さない。そのまま隠すと、次に表示したとき新しいレイヤーが描けるまでの一瞬、前に出していたレイヤーが見える。
  「L1 以上のときだけ表示」で L1 を離して L2 を押すと、L1 が一瞬出てから L2 になっていた（2026-09-19 に実機で見つかった）。
  中身を `Visibility.Hidden` にして 3 回描画を待ってからウィンドウを隠す。表示する側は遅らせない。
  隠す途中に表示を求められたら隠すのをやめる。隠す途中は表示中とみなさない（`IsShown`）

## レイヤー連動（案A）

### 考え方

ZMK のレイヤー切替はキーボード内部で完結し、PC には何も届かない。
そこでキーマップ側で、レイヤーに入るとき **F13〜F24**（通常どのアプリも使わない）を
一緒に押しっぱなしにする。PC 側は `RegisterHotKey` でそれを受ける。

`RegisterHotKey` は登録したキーを他アプリに届かせないので、
**合図キーの検知と抑止を同時に satisfy する**。

### なぜ低レベルフックを使わないか

`SetWindowsHookEx(WH_KEYBOARD_LL)` は全キー入力を監視するため、
EDR / DLP からキーロガーとして検知されうる。会社 PC で使う前提では避ける。
`RegisterHotKey` は「特定キーを OS に予約する」公式 API で、行儀が良い。

### PC 側

```
RegisterHotKey(hwnd, id, MOD_NOREPEAT, VK_F13 + n)   // レイヤー n ごとに登録
WM_HOTKEY 受信          → レイヤー n を表示
GetAsyncKeyState で監視 → 離されたら非表示
```

表示モードは設定で選べるようにする:
- `hold` — 押している間だけ表示
- `toggle` — 押すたびに ON/OFF

**修飾キーの組み合わせも押さえる。** RegisterHotKey は修飾キーが完全に一致したときだけ反応する。
合図キーを修飾なしでしか押さえていないと、ホームロウモッドの Ctrl を押したままレイヤーに入ったとき
（PC には Ctrl+F13 として届く）に表示されず、しかも F13 が作業中のアプリに漏れる。
そこで同じキーを修飾キーの全組み合わせ（16 通り）で登録する（`HotKeyService.TryRegisterAnyModifiers`）。
修飾なしの登録だけは必須で、ほかは取れた分だけ使う。離されたかの判定は GetAsyncKeyState なので、修飾キーに影響されない。
設定画面では、合図キーと同じキーは修飾キーが違ってもショートカットに選べない。

### ZMK 側

**前提**: Pyuron のレイヤー入口は 4 つとも `&lt`（layer-tap）である。

```
pos 34  &lt L_SYM  INT5     pos 35  &lt L_NAV SPACE
pos 37  &lt L_FUNC ENTER    pos 38  &lt L_SYS  INT4
```

`&lt` はホールドが確定したときだけレイヤーに入るため、単純な `&mo` 置き換えでは足りない。
合図キーは **hold-tap のホールド側**に仕込む。

制約がひとつある。**ZMK の hold-tap は param1 を hold 側、param2 を tap 側へ
固定で渡す実装**で、`#binding-cells` は必ず `<2>`、hold 側のビヘイビアは
引数をちょうど 1 つ取る必要がある。したがって合図キーはマクロ側に焼き込み、
レイヤー番号だけを引数で渡す。

```c
/ {
    macros {
        mo_sig_sym: mo_sig_sym {
            compatible = "zmk,behavior-macro-one-param";
            #binding-cells = <1>;
            wait-ms = <0>;
            tap-ms = <0>;
            bindings
                = <&macro_press &macro_param_1to1 &mo MACRO_PLACEHOLDER>
                , <&macro_press &kp F13>
                , <&macro_pause_for_release>
                , <&macro_release &kp F13>
                , <&macro_release &macro_param_1to1 &mo MACRO_PLACEHOLDER>;
        };
    };

    behaviors {
        lt_sym: lt_sym {
            compatible = "zmk,behavior-hold-tap";
            #binding-cells = <2>;
            bindings = <&mo_sig_sym>, <&kp>;
            flavor = "tap-preferred";
            tapping-term-ms = <200>;
        };
    };
};
```

使い方: `&lt L_SYM INT5` を `&lt_sym L_SYM INT5` に置き換える。引数の並びは変わらない。

`wait-ms` / `tap-ms` を 0 にするのは必須。既定のままだとバインディングの間に
100ms 入り、レイヤー有効化がそのぶん遅れる。`&mo` を合図キーより先に置くのも同じ理由。

`flavor` と `tapping-term-ms` は組み込みの `&lt`（`tap-preferred` / 200ms）に合わせる。
当初ここを `hold-preferred` と書いて焼いていたが、ZMK のソース（`app/dts/behaviors/layer_tap.dtsi`）で
確かめると組み込みは `tap-preferred` だった。2026-09-16 に直した。記憶や推測で書かず、ソースで確かめること。
打鍵感を変えないため。

適用手順は [docs/examples/pyuron/README.md](docs/examples/pyuron/README.md)。

### 合図キーの自動検出

レイヤー番号と合図キーの対応は、キーマップの中身から読み取る（`ZmkKeymapReader.DetectSignalKeys`）。
最初は設定の `signalKeys` に手で書く作りだったが、一般の利用者にそれを書かせるのは無理がある。

合図キーは「`&mo` と `&kp F13〜F24` を含むマクロ」の中で押される。このマクロがキーマップから直接呼ばれるか、
hold-tap の hold 側から呼ばれていれば、合図付きのレイヤーキーとみなす。レイヤー番号は呼び出し側の引数
（`MACRO_PLACEHOLDER` に入る値）から決める。上の `mo_sig_sym` / `lt_sym` の形がそのまま当てはまる。

設定の `signalKeys` は上書き用に残した。書いたレイヤーはそちらが優先で、空文字にすると追従しない。
設定画面で検出と同じ値を選んだときは上書きを持たない。持つと、あとでファームを変えたときに追従しなくなる。

### 重ねたレイヤー

L1 を押したまま L3 に入ることがある（L1 の中に `&mo 3` がある配列など）。押されている合図キーを
1 つだけ覚える作りでは、L3 のキーを離した時点でオーバーレイが消え、L1 のキーはまだ押されているのに
何も出ていない状態になった。いまは押されている合図キーをすべて持つ。

表示するのは、押されているレイヤーのうち**番号が最も大きいもの**。ZMK も番号の大きいレイヤーからキーを探すので、
実際に効いている配置と一致する。以前は「最後に押したもの」を出していて、L2 を押したまま L1 を押すと、
キーボードは L2 のままなのに L1 が表示されていた。

**条件付きレイヤー**（`zmk,conditional-layers`。Corne の「Lower と Raise を同時に押すと Adjust」など）は、
押されているレイヤーの集合から ZMK と同じ規則で入るレイヤーを足してから、番号の最も大きいものを選ぶ
（`Keymap.WithConditionalLayers`）。Adjust に入るキーは無いが、Lower と Raise に合図キーがあれば表示できる。
以前は後から押したほうのレイヤーが出ていた。

### 書き換えの生成

一般の利用者に上の手順（ファイルを置き、include し、行を置き換える）を手でやってもらうのは難しい。
アプリがキーマップを読んで、書き換え済みの全文を作る（`KeymapPatcher`）。利用者は .keymap を 1 つ貼り替えるだけでよい。

- 置き換えるのは、キーマップのレイヤーの中の `&mo` / `&lt` のビヘイビア名だけ
  （`&lt L_SYM INT5` → `&zo_lt_l1 L_SYM INT5`）。引数も桁揃えの空白もそのまま。コメントの中は触らない
- 定義（上の `mo_sig_*` / `lt_*` と同じ形）は、最初の `/ {` の直前に目印付きで差し込む。
  `#include` より後になるので `MACRO_PLACEHOLDER` や `F13` が使える。別ファイルは作らない
- 合図キーは F13 → F16〜F19 → F22〜F24 → F14 → F15 → F20 → F21 の順に割り当てる。キーマップが普通のキーとして使っている
  F13〜F24 と、既に合図キーを持つレイヤーの分は避ける。
  同じキーボードを別の PC につなぐと、合図キーはそのまま届く。macOS は F14 / F15 で画面の明るさを変え、
  Linux（xkeyboard-config の既定）は F20 をマイクのミュート、F21 をタッチパッドの切り替えにしているので、
  この 4 つはほかが尽きたときだけ使う。以前は F13 から順で、2〜3 番目のレイヤーが Mac で明るさを変えていた（2026-09-17 に直した）。
  以前の順で生成した割り当ては、下の「前回生成したレイヤーは同じ合図キーのまま」でそのまま残る
- 前回の生成分（目印の区間と `zo_` の名前）を取り除いてから考えるので、何度かけても同じ結果になる。
  取り除けば元のキーマップにそのまま戻る。初期設定の「うまくいかなかったとき」は、これで書き換えを取り除く（`RemoveGenerated`）。
  手で入れた合図キーは残る
- **書き換えが必要かは、書き換えた結果が今のファイルと同じかで決める**（`KeymapPatch.Changed`）。
  以前は「合図キーを付けるレイヤーがあるか」で決めていて、前回このアプリで書き換えたキーマップでも
  毎回「書き換えが必要」と出ていた。GitHub にコミットして「取り直して確かめる」を押しても、完了したと分からなかった
- `AlreadySignaled` は今のファイルが持つ合図キー（手で入れたものと、前回生成したもの）、`Added` は今回新しく付けるレイヤーだけ
- 前回生成したレイヤーは同じ合図キーのままにする。後からレイヤーキーを足したとき、既存の割り当てがずれると
  書き込み済みのファームと PC 側の対応が食い違う
- 前回の生成分があるファイルでも「変更点を見る」が出るよう、比べるときはその区間を除き、行番号は元のファイルの行に直す
- 生成するビヘイビアに `display-name` を付ける（組み込みの `&mo` / `&lt` と同じ）。ZMK Studio の一覧に名前で出るように。
  マクロと hold-tap のバインディング定義が `behavior-metadata.yaml` を含むことは ZMK のソースで確かめた
- 条件付きレイヤーは、`if-layers` がすべて合図付きなら「付けられない」に入れず、組み合わせで表示されると伝える
- `sensor-bindings` はレイヤーのキーではないので、置き換えの対象にしない
- hold-tap は組み込みの `&lt` と同じ `tap-preferred` / 200ms。キーマップが `&lt { … }` で変えていれば、
  その中身を既定値の後ろに写す（devicetree では後に書いたものが勝つ）
- 付けないもの: `&tog` / `&to` / `&sl`（押しているあいだ、という仕組みに合わない）、
  自作の hold-tap で入るレイヤー（押し心地の設定を正しく引き継げない）、キーで入らないレイヤー。
  理由を付けて返し、画面で見せる

devicetree としてビルドが通るかはテストでは分からない。テストでは、読み直すと合図キーが見つかること、
見た目が元と同じであること、2 回かけても同じで元に戻せることを確かめている。

### 連動できないレイヤー

**L4 MOUSE は原理的に検知できない。** このレイヤーは `zip_mouse_temp_layer`
（input-processor）がトラックボール操作で自動 ON にするもので、キー押下を経由しないため
マクロを差し込む場所が無い。案 B（ファームから HID 通知）でのみ取得可能。

ただし L4 は 16/17/18 のクリック 3 つ以外すべて `&trans` で、L3 FUNC と同一配置。
表示する価値が小さいため実害は無いと判断し、**L4 は非対応とする**。

### レイヤー ⇔ 合図キー割り当て

| レイヤー | 入口 | 合図キー |
|---|---|---|
| L1 SYM | pos 34 hold | F13 |
| L2 NAV | pos 35 hold | F14 |
| L3 FUNC | pos 37 hold | F15 |
| L4 MOUSE | （自動） | — 非対応 |
| L5 SYS | pos 38 hold | F16 |

## 要検証（実機で確かめる）

| # | 項目 | 落ちたときの代案 | 状態 |
|---|---|---|---|
| V1 | `RegisterHotKey` で予約したキーが `GetAsyncKeyState` で拾えるか | 拾えなければ hold モードを諦め toggle のみにする | ✅ 成立（合成入力） |
| V2 | `AllowsTransparency` の描画コスト | `UpdateLayeredWindow` に差し替え | |
| V3 | hold-tap の hold 側にマクロを置いて `&mo` を press/release できるか | レイヤーごとに個別マクロを定義する | ✅ 実機で成立 |
| V4 | 会社 PC で未署名 exe が動くか / 配置場所 | framework-dependent 版、配置先の変更 | |

### V1 の検証結果

`tools/HotkeyProbe` で測定した。`SendInput` で F13 を 300ms 押しっぱなしにしたときの値。

| 項目 | 登録なし（対照） | `RegisterHotKey` で登録 |
|---|---|---|
| WM_HOTKEY 受信回数 | 0 | 1 |
| WM_HOTKEY 到達 | — | 1.1 ms |
| `GetAsyncKeyState` 押下 | 3.1 ms | 1.1 ms |
| `GetAsyncKeyState` 解放 | 330.1 ms | 318.5 ms |

**予約したキーでも押下・解放の両方が観測できる。** hold モードは実装可能。

分かったこと:

- `MOD_NOREPEAT` は効いている。300ms 押しっぱなしでも WM_HOTKEY は 1 回だけ
- 解放の検知遅延はポーリング間隔にほぼ等しい（測定値の 18ms は
  `Thread.Sleep(1)` の実分解能 15ms 由来）。
  **アプリ側は 15ms 間隔の `DispatcherTimer` で十分**で、それ以上細かくしても意味がない

### 抑止の検証結果

「登録したキーは他アプリに届かなくなる」も測った（`HotkeyProbe --suppress`）。
自分自身をフォーカスのあるアプリ役にして、F13 を合成したときの受信数。

| | 登録なし（対照） | `RegisterHotKey` で登録 |
|---|---|---|
| フォーカスのあるアプリの `KeyDown` | 1 | **0** |
| WM_HOTKEY | 0 | 1 |

**検知と抑止が RegisterHotKey ひとつで賄える**ことを確認した。
合図キーが他アプリに漏れないので、抑止のために低レベルフックを足す必要はない。

### 残る未確認

上記はいずれも `SendInput` による合成入力での測定で、実機のキーとは経路が完全には同じでない。
`HotkeyProbe --manual <キー>` で実キーでも確かめること。

## ロードマップ

| Phase | 内容 | 状態 |
|---|---|---|
| **0** | JSON 手書きレイアウト + 半透過オーバーレイ + ホットキー ON/OFF | ✅ 完了 |
| **1** | `.keymap` パーサ（`#define` 展開 + `bindings` 解決） | ✅ 完了 |
| **2** | レイヤー連動（案A） | ✅ 実機で動作確認済み |
| 3 | 押下キーのハイライト、設定 UI、スタートアップ登録 | 自動起動のみ完了 |

## 自動起動

スタートアップフォルダにショートカットを置く。レジストリの Run キーでも
同じことはできるが、「インストーラもレジストリ書き込みも無し」を通してきたので
それに合わせた。ショートカットならただのファイルで、利用者が自分で消せる。

`--config` で起動していたときは、その指定をショートカットの引数に引き継ぐ。
引き継がないと、自動起動したときだけ別のキーマップが出ることになる。

情報源はスタートアップフォルダの実体だけにしてある。設定ファイルにも
状態を持つと、片方だけ消えたときにどちらが正しいか決められなくなる。

起動時、ショートカットの参照先の exe が**もう無い**ときだけ、いまの exe に向け直す（フォルダごと移した場合）。
参照先がまだあるときは触らない。配布版と開発版を併用している人のショートカットを、開発版が乗っ取らないように。

## 常駐は 1 つだけ

同じ利用者のセッションで 1 つだけ動かす（名前付きミューテックス `Local\ZmkOverlay.Instance`）。
2 つ目が動くと、ホットキーと合図キーを取れずに「登録できません」が並び、トレイのアイコンも 2 つになる。
自動起動で動いているところに exe をダブルクリックする、というのはよくある。

2 つ目は名前付きイベント `Local\ZmkOverlay.Activate` で 1 つ目に知らせて終わる。1 つ目は設定画面（初期設定の途中ならそちら）を前に出す。
使う人には「もう一度開くと設定画面が出る」ように見える。1 つ目が前に出られるよう、2 つ目は知らせる前に
`AllowSetForegroundWindow` で前面に出る権利を渡す。
`--render*` と `--startup` は常駐しないので、ミューテックスを取らない。

## 起動できなくならないように

常駐アプリは、起動に失敗すると設定画面も初期設定も開けない。利用者に残る手段が「config.json を探して消す」だけになる。
直せるものはアプリの側で直す（`ConfigRecovery`）。

- **キーマップが読めない**（.keymap を移した・消した、サンプルを選んだあとで exe のフォルダを移した、など）:
  サンプルを表示して起動し、理由をトレイと初期設定の「キーマップの場所」に出す。
  このとき設定ファイルは書き換えない。USB メモリのように、次に起動したときには戻っているかもしれないため。
  サンプルまで読めない（data フォルダが無い）ときだけ、起動をやめる
- **設定ファイルが壊れている**（JSON として読めない、空）: `config.broken-<日時>.json` に移して、初回起動として作り直す。
  消さないのは、手で書いた内容を取り戻せるように
- **サンプルの場所が古い**: 設定ファイルが exe の隣なら相対パスで持つ。以前は「サンプルを使う」で絶対パスを書いていて、
  README の案内どおりフォルダを移すと起動できなくなった。古い絶対パスや前の版のサンプル名は、起動時にいまのサンプルへ向け直す
- 初回起動で書き込めるかの確認は使い捨てのファイルで行う。以前は config.json そのものを開いて確かめていて、
  中身を書く前に止まると空の config.json が残った

`--config` で指定したファイルは開発用なので、壊れていても移さず、起動をやめる。

## AltGr と衝突しないショートカット

Windows では AltGr が Ctrl+Alt として扱われる。ドイツ語配列の AltGr+7/8/9/0 は `{ [ ] }` で、
Ctrl+Alt+数字 をホットキーとして押さえると、アプリが動いているあいだその記号が打てなくなる。

そこで、組み合わせを押したときに文字が出るかを、入っているすべてのキーボード配列について調べる
（`KeyboardLayouts.TypesCharacter`。`GetKeyboardLayoutList` と、デッドキーの状態を変えない `ToUnicodeEx`）。
調べるのは Ctrl+Alt を含むもの（AltGr として）と、修飾なしか Shift だけのもの。Win を含むもの、Ctrl だけ・Alt だけのものは文字を出さない。

- 既定の Ctrl+Alt+数字 のうち文字が出るものは割り当てない（`AppConfig.ManualLayerHotkey`）
- 有効 / 無効の既定は、初回起動時に Ctrl+Alt+K → Ctrl+Alt+Shift+K → Ctrl+Alt+F12 の順で最初に使えるものを選ぶ
- 設定ファイルに書かれた組み合わせでも、文字が出るなら登録せずに知らせる
- 設定画面の入力欄は、文字が出る組み合わせを受け付けない。以前は Shift+英字 も受け付けていて、その大文字が打てなくなっていた
- トレイのメニューとオーバーレイのヒントには、実際に登録したものだけを出す

判定は Windows の配列情報が要るので、Core には差し込み口（`HotkeyRules.TypesCharacter`）だけを置き、App が起動時に差し込む。
この PC（日本語 / 英語配列）では Ctrl+Alt+K / 数字 が避けられないこと（誤判定しないこと）は確かめた。
ドイツ語配列などを実際に入れた PC での確認はまだ（配列を足すと利用者の設定が変わるため、開発機では試していない）。

## 押下キーのハイライト

**未着手。このツールの前提と衝突するため、方式の決定が先。**

押されたキーを光らせるには、全キー入力を観測する必要がある。手段は 3 つあり、
risk と工数がまったく違う。

| 方式 | 仕組み | EDR から見たとき | 工数 |
|---|---|---|---|
| 低レベルフック | `WH_KEYBOARD_LL` | **キーロガーとして検知されうる。当初却下した方式そのもの** | 小 |
| Raw Input | `RegisterRawInputDevices` + `RIDEV_INPUTSINK` | フックを仕掛けず入力も横取りしないぶん穏当だが、全キーを取得できる性質は同じ | 中 |
| キーボードから通知 | ZMK にモジュールを足し、キー位置を vendor HID で送る | **OS のキーボード入力を一切監視しない。特定 HID を読むだけ** | 大（ファーム開発） |

会社 PC で使うという前提を崩さずに済むのは 3 番目だけ。
この config は既に `CONFIG_ZMK_STUDIO=y` で vendor HID のパイプが通っているため、
案 B（レイヤー状態の HID 通知）と同じ土俵で、まとめて実装できる。

自宅でしか使わないなら Raw Input で十分。低レベルフックは選ばない。

Phase 2 は実機で通った。ファームを焼いた状態で、キーボードとして正常に動作し、
レイヤーに入るとオーバーレイの表示も追従することを確認済み。
`wait-ms = <0>` を入れたことでレイヤー有効化の遅れも体感されなかった。
ZMK 側の適用手順は [docs/examples/pyuron/README.md](docs/examples/pyuron/README.md)。

## 表示状態の持ち方

最初は「利用者が自分で出した（ピン留め）」と「レイヤーに追従して一時的に出た」を
別物として扱い、ホットキーはピン留めだけを外す作りにしていた。
その結果、ホットキーで消してもレイヤーキーで出てきてしまい、使い物にならなかった。

作り直した現在の形は状態が 2 つだけ。

| | 役割 |
|---|---|
| 有効 / 無効 | ホットキーで切り替えるマスタースイッチ。**無効なら何が起きても出さない** |
| 見せ方 | 有効のときに常時出すか、L1 以上にいるときだけ出すか（`displayMode`） |

| `displayMode` | 無効のとき | 有効のとき |
|---|---|---|
| `layersOnly`（既定） | 出ない | L1 以上にいるあいだだけ表示 |
| `always` | 出ない | 常時表示。レイヤーに応じて中身が変わり、離すと L0 に戻る |

表示するかどうかの判断は `OverlayController` の `AtRest()` と `BeginTransient()` の
2 か所だけに置いてある。判断を散らすと「無効なのに出ている」状態が簡単に生まれる。

### 気をつけた点

- **初期状態は有効。** 無効から始めると、初回起動時にホットキーを押すまで
  何も起きず、機能が存在しないように見える
- **切り替えたことが分かる手段が要る。** `layersOnly` では無効にしても
  画面上は何も変わらない（もともと出ていない）ため、
  トレイのツールチップに状態を出し、切り替え時はバルーン通知も出す
- `Ctrl+Alt+<番号>` で手で選んだレイヤーは、**キーボードのレイヤーを使った時点で忘れる**。
  残したままにすると「L1 以上のときだけ表示」にしているのに出しっぱなしになり、
  なぜそうなったのか画面から分からなくなる
- レイヤー直接指定のホットキーは、表示中ではなく**有効なあいだ**押さえる。
  表示に連動させると、素の状態では押さえておらず使えないうえ、
  レイヤーキーを叩くたびに登録と解除を繰り返すことになる

### 検証

この規則はホットキー・レイヤー追従・ウィンドウの可視状態が噛み合った結果なので、
単体テストでは囲えない。実アプリを合成キーで動かす
[tools/verify-overlay.ps1](tools/verify-overlay.ps1) で 25 項目を確認している
（重ねたレイヤーの 3 項目、Ctrl を押したままの合図の 2 項目、2 重起動の 2 項目を含む）。
常駐は 1 つだけなので、スクリプトは最初に動いているものを終了させる。

**画面がロックされていると動かない。** 入力デスクトップが別になり、
合成キーは `GetAsyncKeyState` には映るのに `WM_HOTKEY` が配送されないため、
全項目が FAIL に見える。実際にこれで一度、あるはずのない回帰を疑った。
スクリプトはロックを検出したら中断する。

**外から合図キーの押下を観測することはできない。** アプリがホットキーとして
押さえているあいだ、別プロセスの `GetAsyncKeyState` はそのキーを
「離れている」と返す。アプリ自身は観測できる（hold モードが成立するのはそのため）。
判定はウィンドウの可視状態だけで行うこと。キー状態を見る診断は嘘をつく。

**残っている不安定さ。** 手動レイヤーの経路で、5 回に 1 回ほど
「押しているあいだ表示される」が落ちることがある。原因は特定できていない。
合図キーの押下を一度も観測できないまま猶予（`graceMs` = 150ms）を過ぎると
畳む作りなので、状態の読み取りが一時的に失敗すると早すぎる非表示が起こりうる。
次の押下で復帰するため実害は小さいが、未解決として残す。

## トレイ

操作の受け皿はトレイだけなので、初めて使う人がそこで迷わないことを優先した。

- 専用アイコン（青いタイルに 4 つのキー。左上だけ琥珀色で、押しているレイヤーキーを表す）。
  `tools/make-icon.ps1` がコードから描いて `Assets/app.ico` を作る。画像編集ソフトなしで直せて、結果も再現できる
- 無効のあいだはアイコンの色を抜いて薄くする。ツールチップを待たなくても状態が分かる
- メニューの見出しに状態を出す。「レイヤーを表示」から各レイヤーを手で出せるので、
  キーボードを書き換える前でも一通り見られる。ショートカットは項目の右側に並べて、覚えるきっかけにする
- 左クリックでもメニューを開く。NotifyIcon は右クリックのメニューにしか公開 API が無いため、
  内部の `ShowContextMenu` を呼んでいる（自前で `Show` すると、メニューの外をクリックしても閉じない）。
  ダブルクリックに別の動作を割り当てるのはやめた。1 回目のクリックで先にメニューが開いてしまう
- 通知は減らした。再読み込みの成功は画面を見れば分かるので出さない。有効/無効の通知は、
  見た目が変わらない「L1 以上のときだけ表示」のときだけ出す。
  警告はバルーンを見逃すと二度と見られないので、メニューにも件数付きで残す

exe のアイコンから取り出すと 32px の面しか得られず、高 DPI のトレイでぼやける。
同じ .ico を埋め込みリソースにもして、トレイの大きさに合う面を選んでいる。
.ico の 256px 以外の面は PNG ではなく 32 ビット DIB で持つ。`System.Drawing.Icon` が
小さい面の PNG 圧縮を確実には読めないため。

「設定…」で設定画面を開く。警告のバルーンやメニューからは、警告一覧のあるキーボードのページを開く。

## 設定画面

config.json を手で編集しなくて済むようにする。トレイの「設定…」から開く。

| ページ | 内容 |
|---|---|
| キーボード | キーマップと物理レイアウトのファイル、PC の配列（JIS / US）、レイヤー名、読み込みの警告 |
| 表示 | 表示するとき（L1 以上 / 常に）、大きさ・不透明度・位置・余白、実際の描画でのプレビュー |
| レイヤー追従 | 追従の有無、hold / toggle、レイヤーごとの合図キー、受け取った合図の ✓ 表示 |
| ショートカット | 有効 / 無効キーの記録、Ctrl+Alt+数字 の有無 |
| 全般 | 表示言語、サインイン時に起動、設定ファイルの場所、バージョン |

- **変更は即時反映して自動保存**。OK / キャンセルは置かない。オーバーレイは結果がすぐ見えるので、
  確定操作を挟むと「押し忘れて効いていない」が起きる
- **反映の経路は 1 本**（`App.TryApply`）。トレイの見せ方切り替え・設定画面・再読み込みはすべてここを通る。
  先にキーマップを読み、読めたときだけ反映して保存する。読めない設定を保存すると次回起動できなくなる
- 設定画面は `AppConfig.Clone()` した複製を書き換えて渡す。失敗したら複製を捨て、画面の値を元に戻して理由を下に出す
- スライダーは止まってから反映する（250ms）。反映のたびにパネルを作り直すため
- キーマップの読み込みに関わる値（`layoutFile` / `keymapFile` / `zmk` / `keyboardLayout` / `language`）が変わっていなければ、
  ファイルは読み直さず前回の結果を使う。キーマップをダウンロードフォルダのような大きな場所に置かれると、
  物理レイアウト探しのたびにフォルダを歩くことになり、スライダーを動かすたびに固まった。
  ファイルの中身が変わったと分かっているとき（「再読み込み」、GitHub からの取り直し、書き換えの保存後）は読み直しを指定する
- ショートカットの入力中は登録済みのホットキーを外す。外さないと、いまの組み合わせを押しても
  OS に横取りされて入力欄に届かない。外している最中に反映が走っても、登録は外し終わるまで保留する
- 配置の分からない .keymap を選んだら、build.yaml からキーボードが分かるときは ZMK 本体から取るかを尋ね、
  分からなければキーの並びが書かれた .dtsi を選んでもらう
- キーの並びを推定しているか ZMK 本体から取ったときは、「ZMK 本体から取得」の欄を出す。
  シールド名は build.yaml から最初から入れておく。以前の案内「シールドの .dtsi を選ぶ」は、
  定義が ZMK 本体にあるキーボードでは行き止まりだった
- 見た目は .NET 9 からの `ThemeMode="System"`（Windows 11 風、ライト / ダークに追従）を設定画面にだけ使う。
  試験的 API 扱いなので WPF0001 を抑止している
- 表示言語を切り替えても、開いたまま文言が入れ替わる。そのため文言は XAML に書かず `ApplyTexts` で入れている
- 設定画面で値を動かすたびにホットキーと合図キーを登録し直すので、同じ失敗は繰り返し通知しない
- レイヤーを手で出すショートカットもレイヤーごとに選べる（`layerHotkeys`）。最初は Ctrl+Alt+数字 固定だったが、
  数字キーを持たない自作キーボードでは押せない。書かれていないレイヤーは従来どおり Ctrl+Alt+数字（0〜9）を使う
- すでに使われている組み合わせ（有効 / 無効キー、他のレイヤー、合図キー）は受け付けず、何に使われているかを入力欄に出す。
  RegisterHotKey は同じ組み合わせの 2 回目を黙って失敗させるので、登録してから気づくのでは遅い
- オーバーレイのヒントは、既定の Ctrl+Alt+数字 のときだけ「Ctrl+Alt+0–5 レイヤー」と範囲で出す。
  割り当てを変えていたら並べると長くなりすぎるので出さない（トレイのメニューに並ぶ）

「サインイン時に起動」はトレイから設定画面に移した。

### 見た目の確認

`--render-settings <出力先>` で全ページを PNG に書き出せる。画面の外で開いて写すので、利用者の画面には何も出ない。
Windows 11 風テーマの背景は Windows（Mica）が描くため、ウィンドウ自身の絵では透明になる。
書き出すときはテーマに合った地を敷いてから重ねている。

**ビルドに失敗したまま `--render` / `--render-settings` を付けて起動しないこと。** 古い exe はこの指定を知らず、
普通に常駐して戻ってこない。一度これでスクリプトが止まり、開発版がトレイに残った。

## 公開

- ライセンスは MIT。最初の公開は 0.1.0（まず試してもらう段階）
- 配布物は `tools/publish.ps1` で作る。ランタイム同梱の単一 exe（.NET のインストール不要）、
  サンプルの data、README、LICENSE、THIRD-PARTY-NOTICES.md を zip にまとめる。exe の隣に config.json を入れない
  （入っていると、誰が使っても初期設定の案内が開かない）
- 同梱する .NET ランタイムは MIT なので、その LICENSE と THIRD-PARTY-NOTICES を zip の `licenses/` に入れる
  （NuGet の runtime pack から、ビルドに使った版のものを写す）。README が指す画像と `docs/configuration.md` も入れる
- **ライセンスの分からないものは公開物に入れない。** 当初は作者の Pyuron（元の zmk-config にライセンス表示が無い）の
  シールド定義とキーマップを、同梱サンプルとテストの材料にしていた。サンプルは ZMK 本体の Corne（MIT）の配置と
  このリポジトリで書いたキーマップ（`tools/sample/corne-demo.keymap`）に、テストは自作の demo40 と ZMK 本体の Corne に替え、
  git の履歴からも取り除いた
- コミットの作者のメールアドレスは GitHub の noreply にする（公開すると全コミットで見えるため）
- 署名していないので、初回起動時に SmartScreen の警告が出る。コード署名は有料なので、
  README で「詳細情報 → 実行」を案内する
- README は使う人向け（英語 `README.md` / 日本語 `README.ja.md`）。
  開発の話は `docs/development.md`、設定項目は `docs/configuration.md` に分けた
- 画面の画像は `--render-*` で書き出したものを切り抜いて `docs/images/` に置く。
  利用者の PC のパスが写る部分は切り落とす
- 作者の Pyuron に手で合図キーを入れた記録は `docs/examples/pyuron/` に移した。一般の利用者はアプリの案内を使う
- GitHub リポジトリの作成と公開は、利用者が中身を見てから決める（2026-09-16 時点で未実施）。
  リポジトリ名は `utori7/zmk-keymap-overlay`（アプリの報告ボタンが指す。`ProjectLinks`）

### キーボードの書き換えは試験的として出す

書き換えは利用者のファームを変える機能なのに、公開の時点で確かめたのは、ビルドが通ること（「ビルドの確認」）と
作者のキーボードで動くことだけ。ZMK のキーマップは人によって書き方の幅が大きい。
そこで「試験的」と出す（2026-09-17 に決めた）。

目的は、詳しくない人を遠ざけることではなく、何を確かめて何を確かめていないかを伝え、失敗しても戻れるようにすること。
このアプリは詳しくない人が画面の操作だけで使えるように作っているので、遠ざける注記は方針に合わない。
書き換えなくてもショートカットでレイヤーを出せるので、機能を設定の奥に隠すこともしない。

- 初期設定の書き換えのステップの見出しに「試験的」の印を付け、確かめた範囲を書く。設定画面のボタンにも「（試験的）」を付ける
- ビルドが失敗したとき・書き込んだあとで具合が悪いときのために、書き換えを取り除く操作を置く（「初期設定の案内」の節）。
  以前は、保存したファームを書き戻せばキーボードは戻るが、GitHub のキーマップは書き換えたまま残った
- 結果は Issue フォーム（`.github/ISSUE_TEMPLATE/keyboard-update.yml`）で受ける。アプリの報告ボタンは、キーボード名・ZMK の版・
  アプリのバージョンを URL で入力欄に入れて開く（フォームの `id` で埋められる）。zmk-config の場所は入れない。送るかは本人が決める
- うまくいったという報告も頼む。報告は失敗したときにしか来ないのがふつうで、報告が無いことは動いたことの証拠にならない

**外す基準。** 次をすべて満たしたら「試験的」を外す。報告が早く集まるようなら件数を引き上げる。

| 項目 | 基準 |
|---|---|
| 作者以外からの、実機で動いたという報告 | 5 件以上 |
| そのキーボードの種類 | 3 種類以上（例: Corne 系、Lily58・Sofle 系、それ以外） |
| 書き換え方 | GitHub と PC のファイルの両方で、それぞれ 1 件以上 |
| ZMK の版 | 安定版（v0.3 系）と main の両方で、それぞれ 1 件以上 |
| 重い不具合（ビルドが通らない、押し心地が変わった、キーが押されたままになる） | 最後の修正から 4 週間、報告がない |
| これまでに出た失敗 | すべて、直したか、書き換える前に「付けられない」と伝えるようにした |

件数の目安が小さいのは、ZMK × Windows という狭い利用者層で、0.1.0 への報告は少ないと見込むため。
「たまたま動いた」とは言えず、かつ届く見込みのある数にした。

**報告を待たずに様子を見る。** 書き換えたキーマップには必ず `/* ZMK Keymap Overlay: begin (generated) */` が入る。
GitHub にサインインして `"ZMK Keymap Overlay: begin"` をコード検索すると、書き換えを入れた公開の zmk-config が見つかる。
それぞれの Actions でビルドが緑か赤かを見る。数週間たっても区間が残っていれば、使い続けていると見てよい。
見えるのは公開リポジトリだけで、ビルドが緑でも実機で動いたとは限らない。

## 初期設定の案内

設定ファイルが無い状態で起動すると開く。トレイの「初期設定をやり直す…」と、
設定画面のレイヤー追従ページからも開ける。サンプル（Corne）を表示したまま、次の順に案内する。
どのステップも、その場で反映・保存される。途中で閉じても、そこまでの設定は残る。

| ステップ | 内容 |
|---|---|
| ようこそ | 何ができるか、これからの流れ、表示言語 |
| キーマップの場所 | GitHub の URL / PC の .keymap / サンプル |
| キーボードの形 | 実際の描画でプレビュー、キーの並びの出どころ、PC の配列 |
| キーボードの書き換え（試験的） | レイヤーごとの状態（合図キーあり / 付ける / 付けられない理由）、手順、うまくいかなかったときの取り除き方 |
| 動作確認 | レイヤーキーを押すと、合図が届いたレイヤーに ✓。書き換えが入っていれば結果の報告 |
| 完了 | 操作の場所、表示の仕方、サインイン時に起動 |

- **GitHub のとき**は、書き換えた全文をクリップボードに入れて、そのファイルの編集画面を開く。
  利用者は「全選択 → 貼り付け → Commit」だけでよい。コピーの直前に必ず取り直す。
  利用者が GitHub 上で先に何か直していたとき、古い内容で上書きしないため
- **PC のファイルのとき**は、確認してから上書きし、元のファイルを `.bak` で残す。BOM の有無と改行は元に合わせる。
  用意したあとでファイルが変わっていたら上書きせず、作り直す
- 「変更点を見る」で、書き換える行と追加する定義を、貼り替える前に見せる
- 今のキーマップにこのアプリの書き換えが入っていれば、「うまくいかなかったとき」のカードを出す。
  GitHub のときは、取り直してから書き換えを取り除いた全文をコピーし、編集画面を開く（書き換えのときと同じ流れ）。
  PC のファイルのときは、確認してから今のファイルを `.bak` に残して取り除く（上書きと同じ流れ。`OverwriteKeymap`）。
  ここに問題の報告ボタンも置く
- 動作確認のステップにも、書き換えが入っているときだけ結果の報告ボタンを出す。うまくいったという報告も集めたいので
- 書き込み（フラッシュ）の方法はキーボードごとに違うので、アプリは具体的に説明しない。
  今のファームを先に保存しておくことだけを勧める

初回に作る設定ファイルが exe の隣に書けず `%APPDATA%` に置かれたときは、同梱のサンプルを絶対パスで指す。
以前は相対パスのままで、その場合はサンプルが読めずに起動できなかった。

`--render-setup <出力先>` で全ステップを PNG に書き出せる（`--render-settings` と同じ仕組み）。

## オーバーレイの見た目

タイピング中に視界の端に出るものなので、**邪魔にならないこと**を優先している。
ただしレイヤーのタブ一覧・コンボ・操作ヒントの 3 つは、使い方を思い出す手がかりとして残す。

| 要素 | 扱い |
|---|---|
| タブ | 細い 1 行。選択中は背景で強調し、太字にはしない（太字にするとタブ行の長さがレイヤーごとに変わる） |
| 自動で出ないレイヤー | 淡い文字と点線の枠。「このレイヤーは自動追従できません」という文章はやめた |
| 操作ヒント | 覚えておくべき操作だけ（`Ctrl+Alt+K オフ · Ctrl+Alt+0–5 レイヤー`）。合図キーの詳細は設定画面で見る |
| `&trans` | 記号を出さず枠だけ。▽ が並ぶと画面がうるさい |
| キーの文字 | 実測して幅に収まるまで縮める。文字数による段階縮小では "Muhenkan" と "Win⇧←" が揃わなかった |
| 配色 | 彩度を抑え、キーの種類は背景のわずかな色味と文字色で分ける |

既定の `keyUnitPx` は 56 から 44 に下げた。既存の設定ファイルに書かれた値はそのまま使われる。

### 板の大きさを固定する

以前はレイヤーを切り替えるたびにパネルを作り直していた。コンボ行の有無や案内文の長さで
板の大きさが変わり、下端揃えの位置が跳ねて目で追えなかった。
いまは読み込み時に全レイヤーぶんを作って最大寸法に揃え、切り替えは作り置きの差し替えだけにしている。
寸法が変わらないので置き直しも要らない。

板の幅はキー配置の幅に揃える。ヒントはタブと並べて 1 行に収まればタブ行の右端、
収まらなければコンボ行の右端に置く。判定はタブの幅だけで決まるので、全レイヤーで同じ結果になる。

コンボの `layers` は ZMK のレイヤー番号で、以前はキーマップ上の並び順と比べていた。
Pyuron は両者が一致するため表に出ていなかったが、直してある。

## .NET のバージョン

.NET 10（LTS、2028 年 11 月までサポート）。最初は .NET 6 で作ったが、サポートが終わっており、
ランタイム同梱で配布すると修正の止まったランタイムを配ることになるため上げた。
移行の前後で `--render` の出力はバイト単位で一致した。

開発機には SDK をユーザーフォルダ（`%LOCALAPPDATA%\Microsoft\dotnet`）に入れてある。
管理者権限を使わない方針に合わせたもので、システムの `dotnet` コマンドは .NET 6 の SDK のまま。
ビルドしたアプリを動かすための Windows Desktop Runtime 10 は、利用者がシステム全体に入れてある（2026-09-16）。

**ランタイム同梱にしない配り方（framework-dependent）では、.NET 10 が無い PC で起動すると
「.NET をインストールまたは更新する必要があります」というダイアログが出る。** 開発中に実際に出た。
一般の利用者にとっては大きなつまずきなので、公開用の配布物はランタイム同梱（self-contained）にする。

## 表示言語

日本語と英語を持つ。設定 `language`（`auto` / `ja` / `en`）で選び、`auto` は Windows の表示言語に従う。

文言は `ZmkOverlay.Core/Text/Strings.cs`（Core と共通）と `ZmkOverlay.App/Text/UiText.cs`（UI だけ）に、
日本語と英語を並べて持つ。.resx にしなかったのは、単一 exe に衛星アセンブリを抱えずに済むのと、
2 言語を見比べながら直せるため。`T(ja, en)` は引数を 2 つ取るので、片方の書き忘れはコンパイルで分かる。

キーのラベルにも言語で変わるものがある（`無変換` / `Muhenkan`、`M左` / `LMB` など）。
`labelOverrides` は言語より優先する。

テストは日本語の表示を前提に書いてあるので、テストアセンブリの初期化で日本語に固定している。
言語を切り替えるテストは全体で 1 つの状態を触るため、並列実行から外してある。

## Phase 1 — パーサ

`.keymap` と `.dtsi` を直接読んで、手書き JSON と同じモデルに落とす。
実データ（Pyuron）で必要だったものと、どう解いたか。

| 課題 | 解き方 |
|---|---|
| `#define` の展開 | オブジェクト形・関数形の両方を再帰展開。`JP_PLUS` → `LS(SEMI)` のような多段も解く |
| `LS()` / `LC()` / `LG()` / `LA()` の入れ子 | 値としては 1 セルに保ち、ラベル解決時に内側から畳む |
| 位置集合マクロ（`#define KEYS_R 5 6 7`） | 値として展開する。ビヘイビアのプロパティに出るだけなので表示には使わない |
| カスタム hold-tap（`hml` / `hmr` / `htp`） | 定義の `bindings = <hold>, <tap>` と `#binding-cells` から引数を割り振る |
| `combos` | ノードごと読み、ラベルは同じフォーマッタで解決する |
| JIS / US の差 | シフト面テーブルを配列ごとに持ち、設定で切り替える |

### 読まないもの

`#include <...>` のシステムヘッダは展開しない。ZMK のソースが手元にあるとは
限らないため、キーコード名は `KeycodeTable` が内蔵表として持つ。
例外は ZMK の共有レイアウト `<layouts/...>` で、手元にあれば読む（「ZMK 本体から取る」の節）。
`#if` 系の条件分岐も解釈せず、本文をそのまま残して警告に積む。

トップレベルにノード以外のものがあっても、読むのをやめない。

- `/dts-v1/;` や `/delete-node/ &x;` は読み飛ばす。`/omit-if-no-ref/` は後ろのノードへの修飾なので、指示だけ飛ばしてノードは読む。
  `/include/ "x"` はセミコロンが付かないので、文字列まで飛ばす
- 展開できなかったマクロ呼び出し（`ZMK_LAYER(...)` など）は、名前と括弧の中身を飛ばし、警告に積む

以前はトップレベルで解釈できないものに出会うとそこで読むのをやめていて、`/delete-node/` の後ろにあるコンボが
警告も無く消えていた。

zmk-helpers の `ZMK_LAYER(...)` のようなマクロで書いたキーマップは、マクロの定義が手元に無いので読めない。
キーマップのノードが見つからず、`ZMK_` で始まるマクロを飛ばしていたときは、その旨を伝える
（「キーマップが見つかりません」だけでは、何が悪いのか分からない）。

### キーコード名は US 由来であること

ZMK の `AT` は「Shift+2」を送るという意味で、`@` が出るとは限らない。
JIS では `"` になる。したがって別名は**名前ではなく展開先で解決**する
（`AT` → `LS(N2)` → JIS のシフト面 → `"`）。
`EXCLAMATION` のように US と JIS で一致するものもあるが、それは結果論として扱う。

### 解釈できなかったものの扱い

表に無いキーコードや知らないビヘイビアは、**名前をそのまま表示する**。
空白にすると「キーが無い」のか「解釈できなかった」のか画面から区別できない。
構造レベルの問題（レイヤーのキー数が合わない等）は警告として集め、
起動時にトレイから通知する。
